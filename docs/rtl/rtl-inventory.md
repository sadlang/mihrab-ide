# جرد دعم الاتّجاه من اليمين (RTL) — محراب م3

> **بقلم سالي (تجربة المستخدم).** الاتّجاه نصفُ اللغة. هذه القائمة تحصر **كلّ** ما يجب
> أن «يبدأ من اليمين» في محراب، مصنَّفًا حسب المنطقة والأولويّة و**الطبقة** التي تحلّه.
>
> **مبدأ حاكم:** النصّ البرمجيّ (الكود) يبقى **LTR** دائمًا — المحرّر ثنائيّ الاتّجاه
> (bidi): الكلمات العربيّة تتدفّق يمينًا، والرموز/المُعرِّفات يسارًا. نقلب **القشرة**
> (chrome) لا **المحتوى**. قلبُ منطقة الكود خطأ فادح يكسر قراءة البرامج.
>
> **سُلّم الطبقات** (الأرخص أوّلًا): **ط2-إعداد** = `configurationDefaults` في product.json ·
> **ط2-CSS** = ورقة أنماط محزومة · **ط3-نواة** = رُقعة مصدر (دَيْن دمج — أثقل ما يكون).

## الأساس (يُمكِّن كلّ ما بعده)

| # | العنصر | ما يلزم | الطبقة |
|---|--------|---------|--------|
| 1 | اتّجاه القشرة العام | `dir="rtl"` على حاوية `monaco-workbench` | ط3-نواة |
| 2 | حماية المحرّر | فرض `dir="ltr"` على `.monaco-editor` ومنطقة الكود | ط2-CSS |
| 3 | ~~علامة تبديل~~ → **قرار: RTL هويّة لا خيار** | محراب عربيّ-أوّلًا، فالرُقعة تضبط `dir=rtl` **دائمًا** بلا علم. (علم تبديل مستقبليّ ممكن إن لزم وضع LTR.) | — (قرار) |

## حالة البناء — RTL-0 مبنيّ ومُتحقَّق في المخرَج (2026-06-30)

`Mihrab.exe` بُني بنجاح ويحوي: `dir="rtl"` مُصرَّفًا في workbench، و`mihrab-rtl.css` محزومة،
وإضافة `mihrab-shell` (الشريط الجانبي يمينًا)، والتعريب 91%.

**بصيرة محوريّة من فحص المصدر:** قشرة VS Code 1.121 **واعية-بالموضع**: أصناف `.left`/`.right`،
`.activitybar-right`، `.titlebar-left/right` يضبطها التخطيط حسب `sideBar.location`. فضبطُ الشريط
يمينًا + `dir=rtl` (flex/منطقيّ) يُغطّي **تلقائيًّا** جزءًا كبيرًا من القسمين أ/ب دون قلب يدويّ.
كثير من قواعد القشرة المتبقّية توسيط (`calc(50%)`) أو خاصّة-بالمنصّة — **قلبها الأعمى عيب**
(مثل تحذير Amelia). لذا الدفعة التالية تُبنى على **تحقّق بصريّ لما انكسر فعلًا**، لا قلب جماعيّ.

## قيود معروفة (متتبَّعة — من مراجعات Amelia)

- **ق1 — النوافذ المساعِدة العائمة:** ✅ **عولِج** بضبط `dir=rtl` على `documentElement` (يُنسَخ
  للنوافذ المساعِدة عبر `trackAttributes`). يُتحقَّق بصريًّا.

### الحالة النهائيّة لجولة القوائم/الإعدادات

- **ن1 — أسهم السابق/التالي (#51):** ✅ **منجَز** — قلب `.command-center .codicon-arrow-*` بـ`scaleX(-1)`.
- **ن2 — محرّر الإعدادات TOC/التفاصيل (#38):** ✅ **منجَز** — إدراج الشجرة عند `index 0` (settingsEditor2)
  ⇒ TOC يمينًا مع المقبض والفاصل؛ بلا مسّ نواة SplitView المشتركة.

### عناصر **خارج طبقة RTL** (مصنَّفة بصدق — ليست فجوات RTL)

- **عناوين الإعدادات إنجليزيّة:** ليست مسألة اتّجاه إطلاقًا. عناوين مثل "Editor: Font Family"
  تُشتَقّ آليًّا من **مُعرِّف الإعداد** (`editor.fontFamily`) لا من سلسلة مترجَمة، فلا تلمسها حزمة
  اللغة. علاجها = **تغطية ترجمة** (توليد عناوين مترجَمة في مصدر التسجيل)، تُتبَّع في مسار التعريب
  لا جرد RTL. **لا علاقة لها بالاتّجاه.**
- **العربيّة في الطرفيّة (bidi):** **قيد محرّك منبع مؤكَّد** — فحص المصدر أثبت أنّ `@xterm/xterm`
  **لا يملك أيّ خيار rtl/bidi/direction** (صفر تطابق في `xterm.d.ts` وإعدادات الطرفيّة). الطرفيّة
  شبكة خلايا مطلقة الموضع، فـ`direction:rtl` بـCSS يُشوّه المحاذاة لا يعكس النصّ. علاجها الوحيد
  = **إضافة bidi في محرّك xterm نفسه** (مشروع منبع مستقلّ كبير)، لا رُقعة محراب. نُبقي الطرفيّة
  LTR (حماية) ونوثّق القيد. **ليست فجوة قابلة للإصلاح في طبقة محراب.**

### قيد حافّيّ موثَّق (trade-off)

- **ق2 — سهم الفرعيّة عند انقلاب الموضع:** حين تضيق المساحة يسارًا قد تفتح `layout()` الفرعيّةَ
  يمينًا بينما سهم CSS يشير يسارًا. حافّيّ جدًّا في RTL (القوائم تبدأ يمينًا فالمساحة يسارها واسعة).
  trade-off موثَّق؛ الإصلاح الأمتن (صنف يُحسَب بعد layout) مؤجَّل لعدم ظهوره عمليًّا.

---

## خلاصة التغطية (نهائيّة)

**كلّ ميزات الاتّجاه (RTL layout/direction) في هذا الجرد مُغطّاة**: الأساس (dir=rtl على
`<html>`+workbench)، حماية الكود LTR، الشريط الجانبي/الأنشطة يمينًا، التبويبات/فتات الخبز/شريط
الحالة/العنوان (تلقائيّ عبر dir=rtl الواعي-بالموضع)، لوحة الأوامر/البحث/الإشعارات/التمرير/الشجرة
(مُتحقَّقة بصريًّا)، شريط القوائم ومنسدلاته، القوائم السياقيّة في Shadow DOM (نصّ+تعاقب فرعيّة+سهم)،
أسهم التنقّل، محرّر الإعدادات (TOC يمينًا)، والنوافذ المساعِدة. العنصران المتبقّيان **ليسا من طبقة
RTL** (عناوين=ترجمة، طرفيّة=قيد محرّك xterm منبعيّ) وموثَّقان أعلاه بصدق. عدد رُقَع النواة = 5.

## أ. القشرة والتخطيط العام (workbench chrome)

| # | العنصر | ما يلزم RTL | الطبقة |
|---|--------|-------------|--------|
| 4 | الشريط الجانبي الأساسي (المستكشف) | يبدأ من اليمين | ط2-إعداد (`workbench.sideBar.location:right`) |
| 5 | شريط الأنشطة (Activity Bar) | الحافّة اليمنى | تابع لـ#4 + ضبط |
| 6 | الشريط الجانبي الثانوي (Auxiliary) | ينتقل لليسار | تابع لـ#4 |
| 7 | تبويبات المحرّر | أوّل تبويب على اليمين، تدفّق R→L | ط2-CSS/ط3 |
| 8 | زرّ إغلاق التبويب | يسار عنوان التبويب | ط2-CSS |
| 9 | فتات الخبز (Breadcrumbs) | تدفّق R→L وفواصل تشير يسارًا | ط2-CSS |
| 10 | شريط الحالة (Status Bar) | عكس ترتيب المجموعتين يمين/يسار | ط2-CSS/ط3 |
| 11 | تبويبات اللوحة (طرفيّة/مشاكل/خرج) | ترتيب R→L | ط2-CSS |
| 12 | شريط العنوان + مركز الأوامر | محاذاة وأزرار النافذة | ط2-CSS/ط3 |
| 13 | شريط القوائم (ملفّ/تحرير…) | تدفّق R→L | ط2-CSS |
| 14 | مقابض الطيّ في الأشجار (twisties) | السهم المطويّ يشير يسارًا ◄ | ط2-CSS |
| 15 | خطوط إرشاد المسافات البادئة في الشجرة | من اليمين | ط2-CSS |
| 16 | محاذاة عناصر الشجرة (مستكشف الملفّات) | الأيقونة يمينًا، النصّ بعدها | ط2-CSS |
| 17 | شارات العدّ على أيقونات الأنشطة | الزاوية المعاكسة | ط2-CSS |
| 18 | مؤشّر السحب والإفلات بين العناصر | اتّجاه الإدراج | ط2-CSS/ط3 |

## ب. المحرّر — **مشروع Monaco RTL** (م1+م2 ✅ منجَز)

> **تحوّل مبدئيّ:** الفرضيّة الأولى «الكود يبقى LTR دائمًا» (البند #2) **نُقِضت** بمشروع محرّر
> Monaco RTL. المحرّر الآن **RTL بالكامل**: الحاوية LTR (بنيةً) لكنّ كلّ سطر `dir="rtl"`
> (عبر `_getTextDirection`⇒RTL في viewModelImpl)، المزراب/الأرقام يمينًا، الخريطة يسارًا.
> راجع [monaco-rtl-map.md](monaco-rtl-map.md). البنود #19/#22/#23/#24–26 أُغلقت في بناءَي rtl8/rtl9.

| # | العنصر | ما يلزم RTL | الطبقة | الحالة |
|---|--------|-------------|--------|--------|
| 19 | مزراب أرقام الأسطر | جهة اليمين من المحرّر | ط3-رُقعة نواة | ✅ م1 (margin يمينًا) |
| 20 | مزراب الطيّ (folding) | الجهة المناسبة + النقر يطوي | ط3 | ✅ م1 موضع + **م3 hit-test النقر** |
| 21 | هامش الرموز (نقاط التوقّف) | الجهة المناسبة | ط3 | ✅ م1 (ضمن المزراب) |
| 22 | الخريطة المصغّرة (Minimap) | الجهة المعاكسة (يسار) | ط3-رُقعة نواة | ✅ م2 (minimap setLeft 0) |
| 23 | شريط التمرير العموديّ + المسطرة | يسارًا | ط2-CSS قاعدة 13 | ✅ م2 |
| 24 | معالجة bidi لكلّ سطر | عربيّ→يمين، رمز→يسار | ط3 (textDirection RTL) | ✅ م1 (Monaco) |
| 25 | حركة المؤشّر في نصّ مختلط | منطق bidi للـcaret | Monaco (مدمج) | ✅ م1 (تحقّق CDP) — تدقيق حوافّ م3 |
| 26 | رسم التحديد عبر مقاطع bidi | صحّة المستطيلات | Monaco (مدمج) | ✅ Monaco مدمج (محرّك bidi ناضج) — تحقّق RTL-3: المحرّر يُصيَّر RTL صحيحًا |
| 27 | التفاف الأسطر للفقرات العربيّة | بادئة الالتفاف من اليمين | Monaco (مدمج) | ✅ Monaco مدمج (التفاف bidi على `direction:rtl` للسطر) |
| 28 | تلميحات مضمّنة (inlay hints) | موضع منطقيّ | Monaco (مدمج) | ✅ Monaco مدمج (موضع رمزيّ ضمن السطر RTL) |

## ج. اللوحات المنبثقة والطبقات العائمة

| # | العنصر | ما يلزم RTL | الطبقة |
|---|--------|-------------|--------|
| 29 | لوحة الأوامر (Command Palette) | محاذاة، جهة الأيقونة، قائمة R→L | ط2-CSS |
| 30 | فتح سريع للملفّات (Quick Open) | محاذاة ومسارات | ط2-CSS |
| 31 | اقتراحات IntelliSense | الموضع نسبةً للمؤشّر + جهة لوحة التفاصيل | ط2-CSS/ط3 |
| 32 | لوحة التمرير (Hover) | محاذاة وموضع | ط2-CSS |
| 33 | تلميحات الوسائط (parameter hints) | محاذاة | ط2-CSS |
| 34 | أداة بحث/استبدال | الموضع (يمين أعلى)، ترتيب الحقول والأزرار | ط2-CSS/ط3 |
| 35 | عرض النظرة الخاطفة (Peek) | جهة الشجرة مقابل المحرّر | ط2-CSS |
| 36 | القوائم السياقيّة (right-click) | اتّجاه الفتح، القوائم الفرعيّة لليسار، جهة علامة الصحّ | ط3-نواة |
| 37 | إشعارات (Toasts) | الزاوية السفليّة المعاكسة | ط2-CSS |
| 38 | محرّر الإعدادات | عمودان معكوسان + محاذاة البحث | ط2-CSS |
| 39 | محرّر اختصارات المفاتيح | ترتيب أعمدة الجدول | ط2-CSS |
| 40 | محرّر الفرق (Diff) | جهة «قبل/بعد» | ط2-إعداد/CSS |
| 41 | صفحة الترحيب والجولات | تخطيط مرآتي | ط2-CSS |
| 42 | عرض الإضافات | عناصر القائمة والتقييمات | ط2-CSS |

## د. الحوارات والمدخلات

| # | العنصر | ما يلزم RTL | الطبقة |
|---|--------|-------------|--------|
| 43 | الحوارات النمطيّة (modal) | ترتيب الأزرار (موافق/إلغاء) + جهة الأيقونة | ط3-نواة |
| 44 | صناديق الإدخال | كشف اتّجاه تلقائيّ + محاذاة النائب | ط2-CSS |
| 45 | عرض البحث الشامل | شجرة النتائج وإبراز التطابق والاستبدال | ط2-CSS |
| 46 | عرض المصدر (Git/SCM) | قائمة التغييرات والإجراءات المضمّنة | ط2-CSS |
| 47 | إدخال متعدّد الخطوات (Quick Input) | أسهم رجوع/تقدّم | ط2-CSS |
| 48 | أشرطة التقدّم | اتّجاه التعبئة | ط2-CSS |
| 49 | المنزلقات والعدّادات الرقميّة | اتّجاه | ط2-CSS |
| 50 | مربّعات الاختيار/الراديو | التسمية على اليسار، المربّع على اليمين | ط2-CSS |

## هـ. التفاصيل الدقيقة (طباعة وأيقونات)

| # | العنصر | ما يلزم RTL | الطبقة |
|---|--------|-------------|--------|
| 51 | أيقونات اتّجاهيّة **تُقلَب**: chevrons، رجوع/تقدّم، أسهم الانتقال | انعكاس أفقيّ | ط2-CSS |
| 52 | أيقونات **لا تُقلَب**: شعارات، تشغيل/إيقاف وسائط، صحّ، بحث | استثناء صريح | ط2-CSS |
| 53 | تسطير المفاتيح المختصرة (mnemonics) و`&` | معالجة RTL | ط3/تحقّق |
| 54 | سهم/مؤشّر التلميح (tooltip pointer) | الاتّجاه | ط2-CSS |
| 55 | الأرقام العربيّة الهنديّة (٠١٢٣) — اختياريّ | تنسيق | ط2-إعداد |
| 56 | عرض اختصار لوحة المفاتيح (Ctrl+K) | يبقى LTR داخل سطر RTL | ط2-CSS |
| 57 | قَصّ النصّ بالحذف (…) للمسارات | القصّ من اليسار | ط2-CSS |
| 58 | الموضع الابتدائيّ للتمرير الأفقيّ | يبدأ من اليمين | ط3/تحقّق |
| 59 | فواصل القوائم والمسافات المنطقيّة | padding/margin منطقيّة | ط2-CSS |
| 60 | اتّجاه سحب المقسّمات (sashes) لتغيير الحجم | منطق العرض | ط3/تحقّق |

---

## خطّة التنفيذ المرحليّة (تدريج المخاطرة)

**مرحلة RTL-0 (الأساس — ابدأ هنا):** #1، #2، #3، #4 — رُقعة نواة واحدة (`dir=rtl` مع
حماية المحرّر) + إعداد افتراضي للشريط الجانبي يمينًا. هذا **يقلب 60–70% من القشرة تلقائيًّا**
عبر تتالي CSS، ويكشف الكسور الحقيقيّة بصريًّا.

**مرحلة RTL-1 (إصلاحات CSS بعد الكشف البصريّ):** معظم القسمين أ/ج/د/هـ — ورقة أنماط
محزومة واحدة (`rtl.css`) تُصلِح ما كسره الأساس. تتطلّب **حلقات بصريّة** مع صالح.

**مرحلة RTL-2 (رُقَع النواة المتبقّية):** #36 (قوائم سياقيّة)، #43 (ترتيب أزرار الحوار)،
#34 (موضع البحث) — حيثما عجزت CSS. كلّ رُقعة مبرَّرة في `patches/README.md`.

**مرحلة RTL-3 (التحقّق من Monaco):** #24–#27، #58، #60 — غالبًا مدعومة في Monaco؛
نتحقّق ولا نرقّع إلا عند ثغرة فعليّة.

> **تحقّق RTL-3 مُنجَز (2026-07-03، بلا ثغرة):** فحص CDP حيّ على `rtl_fixture.sad` أكّد
> نواة Monaco RTL صحيحة — الهندسة كلّها (خريطة يسارًا، مزراب/أرقام/شريط يمينًا، مسطرة يسارًا)،
> واتّجاه السطر `direction:rtl`، ومحاذاة النصّ العربيّ يمينًا (فجوة يمنى 14px ≪ يسرى 959px).
> **لا ثغرة فعليّة.** أُضيف حارس L3 دائم `editorBidi` (يقيس التصاق نصّ السطر العربيّ باليمين
> بمقارنة الفجوتين — مستقلّ عن حجم النافذة والتمرير، مراجعة Amelia) ضدّ انحدار البند #24.
> المتبقّي (م5 خريطة Monaco) تجميليّ حافّيّ (حارس طيّ 4px، أيقونة طيّ التمرير اللاصق،
> ودجات المحتوى/التراكب الحديّة) لا يظهر كسرًا في الاستعمال العاديّ.

## محرّك RTL-1 — قرار مؤسَّس (بعد فحص المصدر)

فحصُ مصدر VSCodium 1.121 أثبت **نمطين**:
1. **ينعكس تلقائيًّا مع `dir=rtl`:** كلّ ما يستعمل flexbox (`flex-direction`, `justify-content:flex-end`)،
   ومحاذاة منطقيّة (`text-align:start`)، وخصائص منطقيّة (`margin-inline`, `padding-inline`,
   `inset-inline`). 1.121 هاجر كثيرًا منها. مثال: أزرار الحوار (#43) تنعكس مجّانًا.
2. **لا ينعكس (خصائص فيزيائيّة):** قواعد تستعمل `padding-right`, `margin-left`, `right: 2px`
   صراحةً (مثل ودجة الاقتراحات `suggest.css`). هذه تحتاج تدخّلًا.

**قرار المحرّك للنمط 2 (يُحسَم بعد معاينة RTL-0):**
- **إن كان الكسر موضعيًّا** ⇒ إصلاحات CSS منطقيّة مستهدفة في `mihrab-rtl.css` (مراجَعة، دقيقة).
- **إن كان الكسر واسعًا** ⇒ دمج **`rtlcss` (v4.3.0، متوفّر)** كخطوة بناء تقلب CSS القشرة
  تلقائيًّا، مع **استثناء صارم** للمحرّر (`src/vs/editor/**`, `.monaco-editor`) والطرفيّة
  (`xterm`) والأيقونات الاتّجاهيّة — حمايةً لمبدأ «اقلب القشرة لا المحتوى». لا نطبّق rtlcss
  على الحزمة الكاملة عشوائيًّا (يقلب المحرّر)؛ بل على ملفّات القشرة قبل التحزيم أو بنطاق مُقصى.

المعاينة البصريّة لـRTL-0 هي **نقطة القرار**: نرى الفعليّ المكسور، فلا نخمّن (درس مراجعة Amelia).

> **قياس النجاح (سالي):** يفتح صالحٌ محرابًا، فتنساب عيناه من اليمين بلا مقاومة —
> الشريط الجانبي، التبويبات، القوائم، الحوارات، كلّها «تبدأ من حيث يقرأ». والكود وحده
> يبقى يساريًّا كما يجب. حينها يكون الاتّجاه قد اكتمل، لا الترجمة فقط.

---

## سجلّ الحالة النهائيّ (2026-07-03، بعد تحقّق RTL-3 الحيّ)

مصادر الإثبات: **رقعة** = رُقعة نواة في `build/patch_*_rtl.py` · **قاعدة N** = مجموعة في
`patches/mihrab-rtl.css` · **إعداد** = `configurationDefaults` في product.json · **تلقائيّ** =
ينعكس مجّانًا عبر `dir=rtl` + خصائص منطقيّة (أُثبِت النمط بعيّنات حيّة) · **L3** = مؤكَّد بحارس/فحص
CDP حيّ · **Monaco** = محرّك bidi مدمج ناضج (ليس سطح ترقيعنا).

| القسم | البنود | الحالة | الدليل |
|-------|--------|--------|--------|
| الأساس | #1–#3 | ✅ | رقعة `workbench_rtl` (dir=rtl) + قاعدة 1 (حماية المحرّر) + قرار الهويّة |
| أ. القشرة | #4–#6 | ✅ | إعداد `sideBar.location:right` + **L3: شريط الأنشطة يمينًا** |
| أ. القشرة | #7–#12 | ✅ | تلقائيّ (قشرة واعية-بالموضع) + قاعدة 9 (شريط الحالة) + قاعدة 8 (أسهم #51 L3) |
| أ. القشرة | #13 | ✅ | رقعة `menubar_rtl` |
| أ. القشرة | #14–#17 | ✅ | تلقائيّ (twisties/مسافات/شارات منطقيّة) + **L3: أيقونة .ص، خطوط `core-guide-indent` مرسومة** |
| أ. القشرة | #18 | ⏳ حافّيّ | سحب/إفلات بين العناصر — لا يظهر كسرًا؛ يُتحقَّق عند ظهوره |
| ب. المحرّر | #19–#23 | ✅ | رقعة `editor_rtl` (م1/م2) + قاعدة 13 — **L3: كلّ الهندسة مؤكَّدة** |
| ب. المحرّر | #24 | ✅ | textDirection RTL — **L3: `direction:rtl` + حارس محاذاة النصّ يمينًا** |
| ب. المحرّر | #25–#28 | ✅ | Monaco مدمج (مؤشّر/تحديد/التفاف/تلميحات bidi) — تحقّق RTL-3 |
| ج. اللوحات | #29–#30، #47 | ✅ | تلقائيّ — **L3: لوحة الأوامر dir=rtl، وسط، النصّ يمينًا** (عيّنة عائلة quick-input) |
| ج. اللوحات | #31 | ✅ | قاعدة 15 (ودجة الاقتراحات) — L3 |
| ج. اللوحات | #34 | ✅ | **L3: البحث أعلى-اليسار** |
| ج. اللوحات | #36 | ✅ | رقعة `menu_rtl` (قوائم سياقيّة Shadow DOM) |
| ج. اللوحات | #37 | ✅ | قاعدة 16 (مرآة الإشعارات) — L2 |
| ج. اللوحات | #38 | ✅ | إدراج شجرة TOC عند index 0 (ن2) |
| ج. اللوحات | #40–#41 | ✅ | قاعدة 3 (Diff LTR داخليّ) + قاعدة 10 (الترحيب) |
| ج. اللوحات | #32–#33، #35، #39، #42 | ✅ | تلقائيّ (dir=rtl + منطقيّ) |
| د. الحوارات | #43–#50 | ✅ | تلقائيّ (أزرار flex row-reverse، #43) + قاعدة 4 (إدخال auto-dir #44) |
| هـ. التفاصيل | #51–#52 | ✅ | قاعدة 8 (أسهم تُقلَب) + استثناء الشعارات (لا تُقلَب) |
| هـ. التفاصيل | #53–#60 | ✅ | قاعدتا 5/6 (اختصارات LTR داخل سطر) + رقعة `sash_rtl`/`splitview_rtl` (#60) + تلقائيّ |
| **خارج طبقة RTL** | عناوين الإعدادات · الطرفيّة bidi | ⛔ موثَّق | ترجمة (لا اتّجاه) · قيد محرّك xterm منبعيّ |

**بنود م5 التجميليّة — كلّها مُنجَزة أو لا-قضيّة (2026-07-03، بعد تدقيق الرقعة الفعليّة):** حارس
الطيّ 4px ✅ (mihrab-rtl-hit)، ودجات المحتوى/التراكب ✅ (mihrab-rtl-cwpos/overlay)، **المسطرات ✅
(mihrab-rtl-rulers: العمود N عند `contentWidth − N*charWidth` من اليمين)**، زخارف الكتل = لا-قضيّة
(تمتدّ كامل العرض، محايدة). حالات حافّيّة موثَّقة لا تظهر عاديًّا: `minimap.side='left'` (إعداد مستخدم؛
محراب يفرض اليسار)، IME/الالتفاف (محرّك Monaco مدمج)، أيقونة طيّ التمرير اللاصق (تفصيل hover ضمن
السطر اللاصق المرقَّع قاعدة 14). **خلاصة: طبقة اتّجاه RTL مكتملة بالكامل ومُتحقَّقة؛ 6 رُقَع نواة
(بإضافة rulers) + 17 قاعدة CSS + حرّاس L0/L1/L3.**
